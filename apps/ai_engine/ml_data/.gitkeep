# This directory holds the trained local classifier model file
# (classifier.joblib), generated by scripts/train_classifier.py.
# The .joblib file itself is gitignored (see .gitignore) since it's
# a build artifact, not source — regenerate it locally by running
# the training script after cloning.